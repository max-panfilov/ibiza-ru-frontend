# Directus API Configuration
# URL вашего Directus API (публичная переменная, доступна в браузере)
PUBLIC_DIRECTUS_URL=http://localhost:8055

# Токен для доступа к приватным данным (опционально для SSG)
# Используется только на этапе сборки для получения неопубликованных материалов
# DIRECTUS_TOKEN=your_admin_token_here
